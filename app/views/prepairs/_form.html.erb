<%= form_with(model: prepair, local: true) do |form| %>
  <% if prepair.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(prepair.errors.count, "error") %> prohibited this prepair from being saved:</h2>

      <ul>
      <% prepair.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :prepair_title %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_field :content, id: :prepair_content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
